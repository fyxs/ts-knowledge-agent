# Agent 项目规范

本目录集中维护所有约束 Agent 参与本项目开发的规范、流程和检查清单。

## 文件职责

- `engineering-rules.md`：工程结构、依赖边界、编码、测试和安全规则的权威来源。
- `workflow.md`：任务开始、实现、验证、审查和交付流程。

## 维护原则

`AGENT.md` 和 `CLAUDE.md` 只做入口索引；通用约束只在本目录维护，避免多份规则漂移。新增规则前先判断是否已有文件可以承载；规则与代码结构变化必须同步更新。
